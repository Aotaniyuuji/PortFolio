<% form_with model: @game do |f| %>
  <h4>
    <%= f.text_field :name %>
  </h4>
  <h4>
    <%= f.text_field :explanation %>
  </h4>
  <%= f.submit '登録' %>
<% end %>

<% @games.each do |game| %>
  <%= game.name %>
  <%= game.explanation %>
<% end %>